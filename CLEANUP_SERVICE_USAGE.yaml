# Run this from Home Assistant Developer Tools -> Actions

action: family_defcon.cleanup_target_button_entities
data:
  remove_old_select_target: true
  remove_family_defcon_target_buttons: true

# After running:
# Restart Home Assistant so the current generated target buttons are recreated.
# Then search Developer Tools -> States for:
# family_defcon_select_target
