# Family DEFCON

Family DEFCON adds a family friendly DEFCON style WiFi timeout system to Home Assistant.

Configuration lives in `/config/family_defcon.yaml`.

After installing through HACS, restart Home Assistant and add:

```yaml
family_defcon:
```

to `configuration.yaml`.
