{
  "config": {
    "step": {
      "user": {
        "title": "Family DEFCON",
        "description": "Set up Family DEFCON. The advanced family/game settings are read from a YAML file in /config.",
        "data": {
          "name": "Name",
          "config_file": "Config file"
        }
      }
    },
    "abort": {
      "already_configured": "Family DEFCON is already configured."
    }
  }
}
