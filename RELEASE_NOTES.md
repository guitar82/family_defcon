# Family DEFCON Release Notes

## v1.1.4

- Rebuilt as HACS compliant GitHub repository and release asset pair.
- Added root `hacs.json` with `zip_release: true` and filename `family_defcon_v1_1_4.zip`.
- Confirmed repo root structure includes `custom_components/family_defcon/manifest.json`.
- Preserved AdGuard Home Assistant integration hybrid connection discovery.
- Preserved direct AdGuard API rule enforcement for Family DEFCON custom rules.
- Preserved parent confirm/cancel actions.
- Preserved case safe status logic.
- Preserved `people_set` setup fix.
