type: vertical-stack
cards:
  - type: markdown
    content: >
      # Family DEFCON

      **DEFCON {{ states('sensor.family_defcon_level') }}**

      Peace Status: **{{ states('sensor.family_defcon_peace_status') }}**

      Last Event: {{ states('sensor.family_defcon_last_event') }}

  - type: entities
    title: Command Controls
    entities:
      - switch.family_defcon_command_system_armed
      - switch.family_defcon_allow_mom_and_dad_targets
      - binary_sensor.family_defcon_mutual_wifi_destruction
      - sensor.family_defcon_daily_launches
      - sensor.family_defcon_conflict_chain
      - sensor.family_defcon_last_launcher
      - sensor.family_defcon_last_target

  - type: entities
    title: Family WiFi Status
    entities:
      - sensor.mom_wifi_status
      - sensor.mom_wifi_minutes_remaining
      - sensor.dad_wifi_status
      - sensor.dad_wifi_minutes_remaining
      - sensor.henry_wifi_status
      - sensor.henry_wifi_minutes_remaining
      - sensor.marc_wifi_status
      - sensor.marc_wifi_minutes_remaining
      - sensor.maggie_wifi_status
      - sensor.maggie_wifi_minutes_remaining

  - type: grid
    title: Parent Controls
    columns: 2
    square: false
    cards:
      - type: button
        name: Clear All
        icon: mdi:restart
        tap_action:
          action: call-service
          service: family_defcon.clear_all
      - type: button
        name: Enforce Now
        icon: mdi:shield-sync
        tap_action:
          action: call-service
          service: family_defcon.enforce_now
