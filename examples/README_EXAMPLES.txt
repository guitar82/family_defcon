Current Family DEFCON examples

Use these files for current installs:

1. dashboard_parent_interface_launcher_style.yaml
   Current parent/admin dashboard.
   Uses the same structure as the launcher:
   PIN -> CONFIRM -> confirmed state -> parent controls.

2. dashboard_parent_interface.yaml
   Same current parent/admin dashboard, kept as the simple/default name.

3. dashboard_parent_interface_working.yaml
   Same current parent/admin dashboard, kept for compatibility with previous instructions.

4. dashboard_launch_console_dynamic_targets.yaml
   Main launcher dashboard with dynamic targets.

5. dashboard_dynamic_target_buttons_only.yaml
   Target button section only.

6. dashboard_status_overview.yaml
   Status/overview dashboard.

Older examples have been moved to:
_archive_old_examples/
